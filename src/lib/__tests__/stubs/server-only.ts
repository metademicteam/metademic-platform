// Empty stub for `server-only` in the Vitest (Node) environment.
// `server-only` throws when imported outside a React Server Component;
// unit tests only exercise the pure logic of services, so resolve it to nothing.
export {};
